import 'package:flutter/material.dart';

class EndgamesScreen extends StatelessWidget {
  const EndgamesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Endgames'),
      ),
      body: const Center(
        child: Text('Endgames - Coming Soon'),
      ),
    );
  }
}
